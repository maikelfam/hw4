<h1><%= @place.name %></h1>

<h2>My Posts</h2>
<% for post in @place.posts %>
  <h3><%= post.title %></h3>
  <h4><%= post.posted_on %></h4>
  <p><%= post.description %></p>
<% end %>

<div class="mb-3", class: "btn btn-success">
  <a href="/posts/new?place_id=<%= @place.id %>">New Post</a>
</div>

<div class="mb-3", class: "btn btn-success">
  <a href="/places">Back to All Places</a>
</div>